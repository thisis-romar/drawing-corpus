# outputs — produced drawings (EMBLEM-NLP-RSPEC-002)

Actual drawing artifacts generated by executing the agent roster. Unlike `../pages/` (the
copyrighted ASUS manual renders, git-ignored), these are **original vector derivatives** and are
committed.

| File | Agent | Drawing | Source evidence |
|------|-------|---------|-----------------|
| `AG-01_connector-map.svg` | AG-01 | C04 interconnect / A14 harness — labelled mainboard connector-location map | page-19 (placement) + page-17 (per-connector close-ups) + page-15/23 (antenna/cable routing) |
| `AG-01_connector-map.png` | AG-01 | PNG preview of the above (rendered via cairosvg) | — |
| `AG-01_interconnect-table.csv` | AG-01 | connector → destination-FRU table | page-19 / page-17 |

**Fidelity:** illustrative and **uncalibrated** (NOT-TO-SCALE). Connector *identities* are
transcribed from manual silkscreen/callouts; connector *positions* are approximate. No pin-level
nets are inferred (RSPEC-002 §9 guardrail) — net-level data requires the acquired boardview (AG-10).

Regenerate (from repo root): `python3 re-h7604/outputs/gen_ag01.py`
